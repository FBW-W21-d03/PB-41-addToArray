const liste = ["Greg", "Flizzy", "Putin", "Hannibal"];
function hinzufügen(name) {
    return liste.push(name)};
    hinzufügen("Mago") + liste.length;
let return1= liste + liste.length;
console.log(return1);


